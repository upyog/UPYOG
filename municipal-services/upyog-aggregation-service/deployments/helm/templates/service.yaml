apiVersion: v1
kind: Service
metadata:
  name: {{ include "upyog-aggregation-service.fullname" . }}
  labels:
    {{- include "upyog-aggregation-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "upyog-aggregation-service.selectorLabels" . | nindent 4 }}
