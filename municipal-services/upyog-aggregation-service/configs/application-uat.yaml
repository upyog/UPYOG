# =============================================================================
# UPYOG Aggregation Service - UAT Environment Configuration
# =============================================================================

server:
  port: 8080
  # The UPYOG API gateway routes /upyog-aggregation-service/** to this pod
  # WITHOUT stripping the prefix, so all routes are served under it.
  contextPath: /upyog-aggregation-service
  readTimeout: 30s
  writeTimeout: 30s
  idleTimeout: 120s
  shutdownTimeout: 15s
  maxRequestBodyMB: 5
  enableCompression: true

redis:
  host: redis-headless.uat
  port: 6379
  password: "${REDIS_PASSWORD}"
  db: 0
  poolSize: 30
  minIdleConns: 10
  dialTimeout: 5s
  readTimeout: 3s
  writeTimeout: 3s
  enabled: true

auth:
  enabled: true
  jwksUrl: "https://keycloak.uat.upyog.org/auth/realms/upyog/protocol/openid-connect/certs"
  issuer: "https://keycloak.uat.upyog.org/auth/realms/upyog"
  audience: "upyog-client"
  tokenExpLeeway: 60
  publicPaths:
    - /health
    - /readiness
    - /liveness
    - /metrics
    - /upyog-aggregation-service/health
    - /upyog-aggregation-service/readiness
    - /upyog-aggregation-service/liveness
    - /upyog-aggregation-service/metrics

observability:
  metrics:
    enabled: true
    path: /metrics
    namespace: upyog
    subsystem: aggregation
  tracing:
    enabled: true
    endpoint: "jaeger-collector.observability:4317"
    serviceName: upyog-aggregation-service
    samplingRate: 0.1

providers:
  defaultTimeout: 10s
  maxRetries: 2
  cacheTTL: 5m
  recentApplicationsSinceDays: 7
  completedServiceStatuses:
    - "APPROVE"
    - "APPROVED"
    - "AUTO_APPROVED"
    - "CANCELED"
    - "CANCELLED"
    - "CLOSEDAFTERREJECTION"
    - "CLOSEDAFTERRESOLUTION"
    - "CLOSURE"
    - "COMPLETED"
    - "CONNECTION_ACTIVATED"
    - "DELIVERED"
    - "DISCONNECTION_EXECUTED"
    - "DISPOSED"
    - "EXPIRED"
    - "MANUALEXPIRED"
    - "REFUNDAPPROVED"
    - "REGISTRATIONCOMPLETED"
    - "REJECTED"
    - "REQUESTCOMPLETED"
    - "REQUESTREJECTED"
    - "RESOLVED"
    - "REVOCATED"
    - "TREE_PRUNING_SERVICE_COMPLETED"
    - "VOIDED"
    - "COMPLETE_REQUEST"
    - "COMPLETEREQUEST"
    - "SETTLED"

rateLimit:
  enabled: true
  requestsPerSecond: 500
  burst: 1000

backend:
  services:
    inbox:
      baseUrl: "http://inbox-service.uat:8080"
      timeout: 10s
      maxConns: 100
      circuitThreshold: 5
      circuitTimeout: 30s
    billing:
      baseUrl: "http://billing-service.uat:8080"
      timeout: 10s
      maxConns: 50
      circuitThreshold: 5
      circuitTimeout: 30s
    property:
      baseUrl: "http://property-services.uat:8080"
      timeout: 10s
      maxConns: 50
      circuitThreshold: 5
      circuitTimeout: 30s
    workflow:
      baseUrl: "http://egov-workflow.uat:8080"
      timeout: 10s
      maxConns: 50
      circuitThreshold: 5
      circuitTimeout: 30s
    tl-services:
      baseUrl: "http://tl-services.uat:8080"
      timeout: 10s
      maxConns: 50
      circuitThreshold: 5
      circuitTimeout: 30s
    user-event:
      baseUrl: "http://egov-user-event.uat:8080"
      timeout: 10s
      maxConns: 50
      circuitThreshold: 5
      circuitTimeout: 30s
    advertisement:
      baseUrl: "http://advertisement-service.uat:8080"
      timeout: 10s
      maxConns: 50
      circuitThreshold: 5
      circuitTimeout: 30s
    draft:
      baseUrl: "http://upyog-draft-service.uat:8080"
      timeout: 10s
      maxConns: 50
      circuitThreshold: 5
      circuitTimeout: 30s
    chb:
      baseUrl: "http://chb-services.uat:8080"
      timeout: 10s
      maxConns: 50
      circuitThreshold: 5
      circuitTimeout: 30s
